const cheerio = require('cheerio');

// put ur stuff here

// run npm start to create bundle.js
// make sure to npm install every package u add (dont just run npm i, run npm install cheerio)
// open the html file in ur browser to run the code.